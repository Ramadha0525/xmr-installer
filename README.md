untuk termux :
  $ apt update && apt upgrade
  $ apt install git nano wget
  $ git clone git@github.com:Ramadha0525/xmr-installer.git
  $ cd xmr-installer
  $ ./installer.sh

di linux/ubuntu :
  $ git clone git@github.com:Ramadha0525/xmr-installer.git
  $ cd xmr-installer
  $ sudo ./installer.sh
